<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alpha.water">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.LoginActivity" android:theme="@style/AppTheme.NoActionBar"/>

        <provider
            android:name=".contentProviders.CustomerContentProvider"
            android:authorities="com.alpha.water.contentProviders.CustomerContentProvider"
            android:exported="false"/>

        <provider
            android:name=".contentProviders.SupplierContentProvider"
            android:authorities="com.alpha.water.contentProviders.SupplierContentProvider"
            android:exported="false"/>

        <provider
            android:name=".contentProviders.OrderContentProvider"
            android:authorities="com.alpha.water.contentProviders.OrderContentProvider"
            android:exported="false"/>
    </application>

</manifest>



























































































<!--
pacoder.com
by Pascal Ngurinzira
-->
